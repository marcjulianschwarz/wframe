// @ui-source: concepts/tokens@0.1.0
// Managed copy. Edits here are local to this app.
// Improvements belong back in the ui repo's concepts/tokens — port
// them there and bump the version. Do not treat this as throwaway code.
type Meta = { title?: string; summary?: string; version?: string };

const meta: Meta = {
  title: "Design Tokens",
  version: "0.1.0",
  summary:
    "The shared token contract (names) vs. per-app values (brand). Components style through these.",
};

export default meta;
